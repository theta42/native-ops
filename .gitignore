# Secrets — never commit
*.env
.env
*.pem
*.key

# Generated edge sites (written by manager)
edge/sites/*.caddy

# OS noise
.DS_Store
Thumbs.db

# Build artifacts
*.tar.gz
*.tar
/tmp/

bin/

# Release binaries — built by .github/workflows/release.yml, never committed
native-ops_v*
/native-ops
dist/
